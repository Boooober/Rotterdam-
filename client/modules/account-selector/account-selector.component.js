import template from './account-selector.template.html';
import controller from './account-selector.controller';

const accountSelectorComponent = {
    template,
    controller
};

export default accountSelectorComponent;
