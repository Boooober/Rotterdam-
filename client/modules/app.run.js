export default function run(Web3Service) {
   Web3Service.init();
   // Web3Service.createContract('hello').then((contract) => {
   //     console.log(contract.greet());
   // });
   // console.log(Web3Service.getAccounts());
}